argument_specs:
  main:
    short_description: Main entrypoint
    options:
      default_user:
        type: str
        default: '{{ vault_user_admin.name }}'
        reqired: yes
        description: Name of the default user. Used by the postfix configuration.
      install_kde_desktop:
        type: bool
        required: False
        description: Install kde-desktop 
      full_ca_cert_path:
        type: str
        required: False
        description: Path to the cert to add to firefox keystore